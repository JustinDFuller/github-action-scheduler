name: "Schedule"
description: "Execute a Schedule"
inputs:
  schedule:
    description: "The Schedule Config (JSON). See schedule.ts for structure."
    required: true
outputs:
  schedule:
    description: "The matched schedule."
runs:
  using: "node20"
  main: "dist/index.js"
branding:
  icon: "calendar"
  color: "orange"
